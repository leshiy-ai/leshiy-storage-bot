# 🚀 Релиз v2.1.6: Распознавание речи и новая панель управления AI

Это обновление расширяет интеллектуальные возможности "Хранилки", добавляя полноценное распознавание аудио и значительно улучшая административную панель для управления AI-моделями.

## ✨ Ключевые нововведения

*   **🗣️ Распознавание речи (Audio-to-Text)**: Теперь бот умеет "слушать"! Он автоматически транскрибирует голосовые сообщения и аудиофайлы, сохраняя текст как AI-описание. Это позволяет искать файлы по содержанию аудио.
    *   Поддерживаются модели `Whisper` (от Cloudflare и BotHub) и `Gemini`.

*   **⚙️ Новая панель управления AI (`/admin`)**: Меню настроек AI полностью переработано. Теперь это интерактивная панель, позволяющая администратору:
    *   **Выбирать модели для разных задач**: Текст, аудио, анализ изображений и видео.
    *   **Видеть наглядную таблицу** с активными моделями для каждого типа задач.
    *   **Мгновенно переключать AI-сервисы** (Workers AI, Gemini, BotHub) и модели без перезагрузки бота.

*   **🧠 Улучшенный семантический поиск (`/search`)**: Внутренняя логика поиска была значительно доработана. Теперь AI лучше понимает сложные запросы, такие как "найди все видео" или "покажи маленькие картинки".

*   **🔌 Поддержка Refresh Token для Google Drive**: Бот теперь сохраняет `refresh_token`, что в будущем позволит автоматически обновлять доступ к Google Drive и избегать повторной авторизации.

## 🏗️ Архитектурные изменения

*   **⚡ Оптимизация загрузки**: Процесс загрузки файлов переработан. Теперь файл скачивается из Telegram только один раз, что значительно ускоряет обработку.
*   **Унификация WebDAV**: Улучшена и стандартизирована логика работы с произвольными WebDAV-серверами.
