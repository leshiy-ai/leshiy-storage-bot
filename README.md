# 🤖 Telegram FTP Storage Bot "Хранилка" by Leshiy
Личный Telegram-бот для автоматической загрузки фото и видео на домашний FTP-сервер. 
Бот поддерживает сохранение исходных имен файлов и автоматическую сортировку по папкам пользователей.

## ✨ Основные функции
* **Универсальность**: Поддержка классического FTP, защищенного SFTP и облачного WebDAV (Яндекс.Диск, Keenetic и др.).
* **Умное именование**: Сохраняет исходные имена для файлов без сжатия и генерирует имена по дате/времени для сжатых фото/видео.
* **Поддержка WEBM**: Возможность сохранять видеофайлы в современных форматах без потери качества.
* **Безопасность**: Доступ к хранилищу имеют только пользователи из белого списка (`ALLOWED_IDS`).
* **Авто-сортировка**: Для каждого пользователя создается отдельная папка на сервере.
* **Диагностика**: Команда `/debug` для проверки статуса подключения к хранилищу в реальном времени.

## 🛠 Технологический стек
* **Язык**: Python 3.11+
* **Библиотеки**: aiogram 3.x, paramiko (SFTP), webdavclient3 (WebDAV)
* **Хостинг**: Render.com (Webhook mode)
* **Хранилище**: Любое хранилище с доступом по FTP, SFTP или WebDAV

## ⚙️ Переменные окружения (Environment Variables)
Для работы бота на Render необходимо задать следующие переменные:
| Ключ | Описание | Пример |
| :--- | :--- | :--- |
| `BOT_TOKEN` | Токен от @BotFather | `12345:AA...` |
| `FTP_HOST` | Адрес сервера с префиксом протокола | `ftp://leshiy-ftp.myserver.ru или sftp://host.ru` или `davs://webdav.yandex.ru` |
| `FTP_USER` | Логин FTP-пользователя | `tgftpbot` |
| `FTP_PASS` | Пароль FTP-пользователя | `your_password` |
| `FTP_FOLDER` | Корневая папка на диске (опционально) | `STORAGE` |
| `ALLOWED_IDS` | ID пользователей кому можно сохранять файлы через запятую | `12345678, 111222` |
| `RENDER_EXTERNAL_URL` | URL вашего сервиса на Render | `https://bot.onrender.com` |
| `RENDER_PORT` | Порт вашего сервиса на Render | `10000` |

**Важно по FTP_HOST**: 
> - Если адрес начинается с `sftp://` — используется протокол SFTP.
> - Если адрес начинается с `dav://` или `davs://` — используется WebDAV.
> - В остальных случаях используется стандартный FTP.

## 📡 Настройка роутера
1. **FTP**: Включить сервер, создать пользователя с правами "Чтение и запись".
2. **NAT**: Пробросить TCP порт 21 на IP роутера или во внутренню сеть.
3. **Firewall**: Разрешить входящие соединения на порт 21.

## 🗄 Настройка хранилища
1. **FTP/SFTP**: Включить сервер на роутере/ПК, создать пользователя с правами "Чтение и запись", пробросить порты (21 для FTP, 22 для SFTP).
2. **WebDAV**: Убедиться, что облачный сервис поддерживает WebDAV и создан пароль приложения.

## 🚀 Развертывание
1. Сделать `git push` в репозиторий.
2. Render автоматически подхватит изменения и выполнит деплой.
3. Проверить статус командой `/debug` в боте.

## 📂 Примеры работы и структура
### Форматы имен файлов:
1. **Сжатое фото/видео**: `photo_20251220_183501.jpg` (автогенерация по дате).
2. **Файл (без сжатия)**: `IMG_4521.HEIC` (сохранение исходного имени).

### Структура папок на диске:
При загрузке бот автоматически создает иерархию:
```text
STORAGE:
 └── FirstName_LastName/
      ├── photo_20251220_183300.jpg
      ├── video_20251220_184015.mp4
      └── original_filename.jpg
```
## 📊 Статус системы
Страница диагностики доступна по пути `/debug`. Она отображает текущую версию бота и состояние подключения к хранилищу, скрывая конфиденциальные данные.

---
**Developed by Leshiy** *Адрес бота: [@leshiy_storage_bot](https://t.me/leshiy_storage_bot)*
